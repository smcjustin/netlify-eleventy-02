<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{{ title }}</title>
    </head>
    <body data-sb-object-id="content/pages/{{slug}}.md">
        <h1 data-sb-field-path="content/pages/{{slug}}.md:title">{{ title }}</h1>
        <main data-sb-field-path="content">
            {{ content }}
        </main>
    </body>
</html>